{
    "servers": {
        "codegraph": {
            "type": "stdio",
            "command": "codegraph",
            "args": ["serve", "--mcp"],
            "cwd": "${workspaceFolder}",
            "autoStart": true
        }
    }
}
